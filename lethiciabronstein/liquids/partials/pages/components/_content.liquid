{% if page.slug == 'lookbook' %}
  {% assign tagNameLoop = '' %}
  {% load_banners position: "banner-lookbook-1" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}

  {% load_banners position: "banner-lookbook-2" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}

  {% load_banners position: "banner-lookbook-3" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}

  {% load_banners position: "banner-lookbook-4" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}

  {% load_banners position: "banner-lookbook-5" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}

  {% load_banners position: "banner-lookbook-6" %}
  {% for banner in loaded_banners %}
    <section class="lookbook-special container">
      <div class="lookbook-special__content">
        <h2>{{ banner.title }}</h2>
        {% if banner.file_url %}
          <img src="{{ banner.file_url }}" alt="{{ banner.title }}" />
        {% endif %}
      </div>
      {% render 'partials/content/home/grid_products', tag_name: 'home-produtos-superior', per_page: 4, infinity: false %}
    </section>
  {% endfor %}
{% endif %}


{% load_banners position: position limit: 1 %}
{% for banner in loaded_banners %}
  <section class="section container content-text">
    <div class="title-wrapper">
      {% if banner.subtitle %}
        <h2 class="title">
          {{ banner.subtitle }}
        </h2>
      {% endif %}
    </div>

    {{ banner.description | replace: '<h1', '<h2 ' }}
  </section>
{% endfor %}